<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PM2 Web UI</title>
  <link rel="icon" type="image/png" href="/static/favicon-32x32.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/static/css/main.css" rel="stylesheet">
  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
</head>
<body class="d-flex flex-column min-vh-100">
  <header>
    <nav class="navbar bg-body-tertiary">
      <div class="container">
        <div class="d-flex align-items-center">
          <img src="/static/favicon-32x32.png" class="pe-3" width="auto" height="30"/>
          <a class="navbar-brand" href="/">PM2 Web UI</a>
          <% if (loggedUser?.role === 'admin') { %>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link" href="/manage-users">Manage users</a>
              </li>
            </ul>
          <% } %>
        </div>
        <% if (loggedUser) { %>
          <div class="d-flex gap-3 align-items-center">
            <%= loggedUser.login %> (<%= loggedUser.role.toLowerCase() %>)
            <a href="/logout" class="btn btn-primary">Log out</a>
          </div>
        <% } else { %>
          <button type="button" class="btn btn-primary">Log in</button>
        <% } %>
      </div>
    </nav>
  </header>
  <main class="container d-flex flex-grow-1 py-3">
    <%- body %>
  </main>
  <footer class="bg-body-tertiary p-4 text-center">
    &copy; <%= nowYear %> by Miłosz Gilga
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
